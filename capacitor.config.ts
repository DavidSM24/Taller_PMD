import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Talleres Jiménez Ortiz',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
